//
// Created by 34043 on 2023/6/25.
//
#include <stdio.h>
int main(){
    //输入半径,求面积
    /*
     * double r,s;
     * printf("请输入半径\n");
     * scanf("%lf",&r);
     * s = 3.14 * r * r;
     * printf("面积为:%.2lf",s);
     */
    //华氏度转摄氏度
    /*
     * double F,C;
     * printf("请输入华氏度温度\n");
     * scanf("%lf",&F);
     * C = (5.0/9)*(F-32);
     * printf("对应的摄氏度为:%lf",C);
     */
    //输入一个四位正整数,逆序打印
    /*
     * int n;
     * printf("请输入一个四位正整数\n");
     * scanf("%4d",&n);
     * printf("%d",n % 10);
     * printf("%d",n / 10 % 10);
     * printf("%d",n / 100 % 10);
     * printf("%d",n / 1000);
     */
    //输入五个学生成绩,输出总分和平均分
    /*
     * double a,b,c,d,e;
     * printf("请输入学生成绩用逗号分隔\n");
     * scanf("%lf,%lf,%lf,%lf,%lf",&a,&b,&c,&d,&e);
     * double sum,avg;
     * sum = a + b + c + d + e;
     * avg = sum / 5.0;
     * printf("总分为%lf,平均分为%lf",sum,avg);
     */
    //输入两个整数,交换他俩
    /*
     * int a,b,t;
     * scanf("%d%d",&a,&b);
     * t = a;
     * a = b;
     * b = t;
     * printf("a=%d,b=%d",a,b);
     */
    return 0;
}